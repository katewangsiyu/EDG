# EDG++ 论文导师批注修订记录

**时间范围**：2026-05-18 ~ 2026-05-19
**涉及文件**：abstract.tex · introduction.tex · related_work.tex · method.tex · experiment.tex · conclusion.tex
**Commit 数**：9 次（e184db4 → 34239a7）

---

## 1. Abstract (`abstract.tex`)

**导师批注摘要**：
- "every prediction" 中式语感；长句太长读起来累；`incur` → `lead to`；`(+4.0% energy, +2.8% force MAE)` 双括号格式难懂；删除 `privileged information`
- 仿 IJCAI 第一句结构（ED 是什么 + 对 MFF 重要 → 现有方法忽略）；推理免 DFT 表述更清；`extend → further propose`；理论一句话；实验段 IJCAI 模板；补结尾应用范围

**执行修改**：
1. **第一句重写**：仿 IJCAI 风格 — "ED 描述电子空间分布，对 MFF 至关重要"
2. **第二句新增**：建立 scientific gap — "现有 MLFF 忽略电子层信息"
3. **删除 `as privileged information`**
4. **推理免 DFT 表述拆分**：训练时蒸馏 / 推理时只用 geometry student
5. **`incur` → `lead to`**
6. **`extend EDG to EDG++` → `further propose EDG++`**
7. **理论分析压缩为 1 句**
8. **实验段重写**：加 IJCAI 模板（"directly integrated + max average improvement 33.7%"）+ 保留 34/36 + 55/60 wins
9. **Chignolin 数字去括号 + qualitative 化**
10. **删除 OOD detector 句**
11. **新增结尾应用范围句**
12. **Keyword 列表删 `privileged information`**

**Commits**: `e184db4`, `37dfee3`

---

## 2. Introduction (`introduction.tex`)

**导师批注**：U1–U8 共 16 条
- U1: 仿 IJCAI 开头加学科覆盖；去破折号
- U2: ED 描述要准确；删反复说同事的多句
- U3: 信息论 mediator 段不该在 challenges 前讲方法
- U4: Challenge 1 改清单式 (1)(2)
- U5: Challenge 2 删 X-ray/neutron（DFT 算基态 ED）；Fig 1(d) 只提 EDG（EDG++ 留 Challenge 3）；末尾加过渡
- U6: Challenge 3 末尾补性能数字
- U7: Contributions 重排（表征→EDG→EDG++→实验→Chignolin），删长篇理论；删 OOD 监测；不说"EDG 恶化基线"避免误导
- U8: Table 1 删 Theory + Analysis 行；Ablations 精简

**执行修改**：

| 单元 | 位置 | 改动 |
|---|---|---|
| U1 | Para 1 | MLFF 开头仿 IJCAI 加"为物理/化学/生物/材料带来革命性进展"，3 句→2 句，去 `---` 破折号 |
| U2 | Para 2 | ED 准确定义为 "probability density of finding an electron at a given point in 3D space"；删 "ED is not merely..."、"Nuclear positions dictate..." 等重复；HK 定理压成 1 句；末尾"three major challenges:" 直接过渡 |
| U3 | Para 3 | 信息论 mediator + symmetry gap + 我们的做法 **整段约 130 词删除** |
| U4 | Challenge 1 | 改清单式："point clouds/voxels suffer from **two limitations**: (1) limited accuracy due to discretization; (2) cost scales with ED resolution" → "To address both, we propose multi-view RGB-D..." |
| U5 | Challenge 2 | 删 X-ray crystallography / neutron diffraction（实验测量），只保留 DFT；Fig 1(d) 句改"equipped with EDG"（EDG++ 不在此提）；末尾加 "However, this naive scheme treats all teacher predictions equally, which motivates the third challenge below." |
| U6 | Challenge 3 | 段末加性能数字："Equipped with this mechanism, EDG++ achieves consistent improvements on 34/36 QM9 task-model combinations and 55/60 rMD17 energy/force tasks, with up to 27–38% tail-error reduction on the hardest 1% of samples." |
| U7 | Contributions | **5 条重排**：(1) multi-view RGB-D + ImageED；(2) Building on ImageED, EDG framework；(3) EDG++（**删长篇理论叙述**）；(4) Validation 34/36 + 55/60（**删"且跨 property 类型与架构稳定"尾巴**）；(5) Chignolin（**改为"EDG++ 缓解 OOD 负迁移"避免误导**；**删 OOD 监测句**） |
| U8 | Table 1 | 删 Theory 行；删 Analysis 行；Ablations 行精简为"$\beta$ (hybrid), $\kappa$ (strictness)" |

**事后修复**：Challenge 1 改写中误引入新破折号 `---far larger than...`，改为 "Unlike atoms in a molecule (typically tens to hundreds), ED is defined..." 结构。

**Commits**: `6186242`, `698ee88`

---

## 3. Related Work (`related_work.tex`)

**导师批注**：
- §2.1 删 (MLFF) — abstract/intro 已定义
- §2.2 删 (ED) — 同上
- §2.3 标题去 `Privileged Information`
- §2.3 末句 "representational and symmetry gap" 读不懂——孤儿声明，正文未兑现

**执行修改**：

| 项 | 改前 | 改后 |
|---|---|---|
| §2.1 line 6 | "Geometric deep learning drives the progress of machine learning force fields (MLFF)" | 删 (MLFF) |
| §2.2 line 10 | "Existing electron density (ED) representations" | 删 (ED) |
| §2.3 标题 | `\subsection{Knowledge Distillation and Privileged Information}` | `\subsection{Knowledge Distillation}` |
| §2.3 末句 | "Our framework distills between qualitatively different representations---ED images and atomic graphs---where the representational and symmetry gap poses distinct challenges." | "Our framework instantiates cross-modal KD by distilling from an image-based teacher to a geometry-based student, providing electron-level inductive bias without requiring ED computation at inference."（仿 IJCAI 风格） |

**Commit**: `0fc1ac0`

---

## 4. Method (`method.tex`)

**导师批注**：
- §3.1 删 (ED)(MFF)
- §3.1 "As the primary output of quantum mechanical calculations" 不对（ED 是计算中间量，不输出）
- §3.1 `privileged information---available` 破折号 → 换句式
- §3.2 "four stages" 与 Algorithm 1 "three stages" 矛盾
- §3.3 措辞："2 million PCQM4Mv2 conformations" / "are processed by DFT" / "Full DFT settings"
- Fig 2 caption (b) 删 (MAE)；(c) `accepts` → `takes`；末尾 "Note that..." 太口语化
- Fig 2 (d) PNG 文件名变更

**执行修改**：

1. **§3.1 Background 段重写**：
   - 旧：5 句重复描述（"ED connects geometry to energy/force"; "primary output of QM calculations"; "HK theorem states ground-state energy"; "computing ρ(r) requires expensive QM"; "treat ED as privileged information"）
   - 新：1 句 LUPI 定位 — "Following the LUPI paradigm~\cite{vapnik2009new,lopez2016unifying}, we treat ED as \emph{privileged information}: it is computed via DFT during training but is not required at inference, eliminating the cost of quantum chemical computation at deployment."
   - 一次性解决：删 (ED)(MFF)、删 "primary output"、删破折号、删与 intro 重复内容

2. **§3.1 four stages → three stages**：
   - 旧：(1) MAE；(2) teacher + predictor；(3) reliability estimator；(4) distill student
   - 新：(1) MAE；(2) **jointly** pre-train teacher + predictor + reliability estimator；(3) distill student
   - 与 Algorithm 1 的 Stage I/II/III 一致

3. **§3.3 三处措辞**（按 IJCAI 调整）：

| 改前 | 改后 |
|---|---|
| "2 million PCQM4Mv2 conformations" | "2 million conformations **from** PCQM4Mv2" |
| "are processed **by DFT**" | "are processed **using DFT calculations**" |
| "**Full** DFT settings" | "**Details of** DFT settings" |

4. **Fig 2 caption**：
   - (b) "masked autoencoder (MAE)" → 删 "(MAE)"
   - (c) "accepts the structural images... transform **it**" → "**takes** the structural images... transform **them**"（顺便修单复数）
   - 末尾："Note that there is no need for explicit involvement of electron density here because the ED teacher has the ability to extract ED-related information." → "Electron density is no longer required at this stage, as the frozen ED-aware teacher already encodes ED-related information from structural images alone."

5. **Fig 2 PNG 文件名**：`EDG++框架图_en.png` → `EDG++框架图.png`（对齐用户更新后的图）

**Commits**: `37dfee3`, `0fc1ac0`, `a44a0f4`, `d7b9829`

---

## 5. Experiment (`experiment.tex`)

**导师批注**：
- Fig 3 caption "Hohenberg-Kohn relevance" 奇怪 → "three categories"
- Fig 5 caption 括号"(166-atom mini-protein substantially out of...)"删
- §4.6 标题 "in the Wild" 不清晰 → "in a realistic OOD setting"
- §4.7 段末 "observed in the wild" 同样
- §4.5 前 `\FloatBarrier` 致 page 9 大块空白

**执行修改**：

1. **Fig 3 caption** (line 59): "Properties are grouped **by Hohenberg-Kohn relevance**" → "Properties are grouped **into three categories**"
2. **Fig 5 caption** (line 219): "Chignolin case study **(166-atom mini-protein substantially out of the ED teacher's training distribution)**" → "Chignolin case study"（删括号）
3. **§4.6 标题** (line 177): "Case Study: Observing Negative Transfer **in the Wild** (Chignolin, 166-atom Mini-Protein)" → "Case Study: Observing Negative Transfer **in a Realistic OOD Setting** (Chignolin, 166-atom Mini-Protein)"
4. **§4.7 段末** (line 191): "**observed in the wild**" → "**observed in a realistic OOD setting**"
5. **§4.5 前 `\FloatBarrier` 删除** (line 125)：rMD17 Tables 3-4 可自然 float；page 9 大块空白消失；**页数 25 → 24**

**Commits**: `0d5d53b`, `d7b9829`, `34239a7`

---

## 6. Conclusion (`conclusion.tex`)

**导师批注**（间接，与 abstract 同款）：
- 双括号数字格式 `($+4.0\%$ energy, $+2.8\%$ force MAE)` 难懂
- "in the wild" 不清晰
- Chignolin 描述要说清楚是 **aggregate 平均**（不是所有构象都负迁移）

**执行修改**：

| 改前 | 改后 |
|---|---|
| "naive EDG measurably degrades the baseline ($+\!4.0\%$ energy, $+\!2.8\%$ force MAE), confirming the negative-transfer hypothesis **in the wild**, while the reliability gating in EDG++ recovers a net improvement ($-\!6.8\%$ energy, $-\!2.9\%$ force MAE)" | "naive EDG worsens the baseline energy MAE by 4.0\% and force MAE by 2.8\% **on average**, while EDG++'s reliability gating reverses this with **mean improvements** of 6.8\% (energy) and 2.9\% (force), **demonstrating that selective distillation mitigates negative transfer in realistic OOD settings**" |

3 处一并修复：双括号格式 → 叙述句；"in the wild" → "realistic OOD setting"；加 "on average" / "mean" 明确是 aggregate。

**Commits**: `37dfee3`, `0d5d53b`

---

## 7. 图片（用户已自行更新）

**点 9 Fig 2 (d) "Molecular graphs"** 视觉问题：
- 原图 panel (d) 左上画的是抽象 graph（圆圈+边），但 student 实际输入是 3D molecular conformation
- 用户**自行重画 PNG**（文件名也从 `EDG++框架图_en.png` 改为 `EDG++框架图.png`），把 (d) 左上换成与 panel (a) 一致的 conformer 风格图标 + label 改为 "Molecular conformations"
- LaTeX 源同步更新文件名引用

---

## 经审核后**未改动**的事项

| 项 | 不改原因 |
|---|---|
| `method.tex` §3.2 "four modules" | "modules" 描述 Fig 2 的 4 个 panel，与 Algorithm 1 的 "three stages" 是不同概念（modules = figure panels；stages = training phases）；IJCAI 也用 "4 main modules"——不矛盾 |
| `experiment.tex` §4.6 "From theory to a stress test"（段首加粗小标） | paragraph-level heading 不是 section title；case study 内部用 stress test 有上下文支持 |
| `Fig 3 property_group_improvement.pdf` | 审核后保留：36-cell heatmap 与 appendix table（aggregated 数值）和 appendix 超参 heatmap 不重复；HK hierarchy 论点有数据支撑（thermodynamic 5/5 win rate across 3 architectures） |
| `incur` 在 method/appendix 技术段落 | 合法英文用词；R1-2 批的是 abstract 措辞偏好，不必全文统一 |
| 大部分 `---xxx---` 破折号 | R2-2 是针对 intro 特定一句；学术写作 em-dash 合法手段，多数保留 OK |
| `privileged information` 在 method/related work/conclusion | 这是你方法的真实 LUPI 框架定位（不是错误用法）；只 abstract 删了（导师说"abstract 里多余"）；其他位置作为学术挂钩保留 |

---

## 全文一致性效果

| 项 | 处理 | 状态 |
|---|---|---|
| "in the wild" | 全文 5 处统一为 "in a realistic OOD setting" / "in realistic OOD settings" | ✅ |
| "privileged information" | abstract 删（含 keyword）；method/related work/conclusion 作 LUPI 定位保留 | ✅ 分层 |
| stages vs modules | stages = 训练 3 阶段（Algorithm 1）；modules = Fig 2 的 4 个 panel | ✅ 区分 |
| Chignolin 数字格式 | 全部叙述化，无双括号；加 "on average" 明确 aggregate 含义 | ✅ |
| HK 定理引用 | intro (一句)、method §3.4 Markov 假设、experiment §4.2、appendix F.1 Evidence 1、conclusion——必要论证保留，不重复 | ✅ |
| Em-dash 破折号 | intro / abstract 大幅清理；其他章节按语境保留 | ✅ |

---

## 排版改善

- Page 9 大块空白 → 4 个 tables 紧凑堆叠
- §4.5/§4.5.1 子标题不再悬空
- 总页数 **25 → 24**

---

## Commit 时间线

| Commit | 内容 |
|---|---|
| `e184db4` | 摘要按导师批注重写 |
| `6186242` | introduction U1–U8 重写 |
| `698ee88` | Challenge 1 去掉破折号 |
| `37dfee3` | abstract/conclusion/method 精简版扫尾 |
| `0fc1ac0` | related_work/method 按导师批注修复 |
| `a44a0f4` | method §3.1 四阶段 → 三阶段 |
| `0d5d53b` | Fig 5/Fig 3 caption + conclusion Chignolin |
| `d7b9829` | Fig 2 PNG 文件名 + FloatBarrier 删除 |
| `34239a7` | "in the wild" → "in a realistic OOD setting" |
